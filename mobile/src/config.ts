export const config = {
  frontend: {
    port: 3000,
    host: 'localhost'
  },
  backend: {
    port: 8080,
    host: 'localhost'
  },
  expo: {
    name: 'soundboard',
    web: {
      port: 3000,
      bundler: 'metro'
    }
  }
};