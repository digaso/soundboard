import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  songItem: {
    backgroundColor: '#333',
    padding: 15,
    marginVertical: 8,
    borderRadius: 5,
  },
  songName: {
    fontSize: 18,
    color: '#fff',
  },
  songAuthor: {
    fontSize: 14,
    color: '#6a0dad',
  },
});

export default styles;
